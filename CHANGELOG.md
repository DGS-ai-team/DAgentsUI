# Changelog

本文件记录 **DAgentsUI** 各标签版本的变更概要；细节以 Git 提交与 PR 为准。

## v0.1.0

首个对外标记版本（`v0.1.0` / `package.json` `0.1.0`）。

- Web：Vite + React 对话工作台，HTTP / SSE 与 DAgents API 联调。
- Electron：桌面壳、内嵌 API 反向代理与用户设置持久化。
- CI：对 `v*` 标签构建含 `dist` + `electron` 等产物的工作流。
