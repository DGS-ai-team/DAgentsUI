# Web / Vite：构建与开发时作为默认 API 根地址（直连后端时生效）
VITE_API_BASE_URL=http://127.0.0.1:8000

# Electron 专用（项目根 .env，主进程 / preload 会读取；复制为 .env 后按需取消注释）
# 真实后端根地址，存在时参与启动解析（与设置页「真实 DAgents API」二选一优先级见 resolveApiBaseUrl）
# API_BASE_URL=http://127.0.0.1:8000

# Electron 内置反向代理监听端口（1–65535）；缺省 37421。修改后需重启桌面应用。
# API_PROXY_PORT=37421

# 主进程日志目录（自动生成）：开发为项目根 log/；Windows 便携包为与 exe 同目录下的 log/；macOS 安装包为系统用户数据目录下的 log/（见应用内 getLogPaths）。
