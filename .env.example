# Web 专用：仅浏览器 / Vite 开发时使用（会写入前端构建产物；Electron 桌面版会忽略此项）
VITE_API_BASE_URL=http://127.0.0.1:8000

# Electron 专用：主进程在「启动时」读取（开发为项目根 .env；安装后为 exe 同目录或 resources/.env）
# 注意：变量名必须是 API_BASE_URL（不是 VITE_API_BASE_URL）；修改后需完全退出并重启桌面应用
# API_BASE_URL=http://127.0.0.1:8000

# Electron 内置反向代理监听端口（1–65535）；缺省 37421。修改后需重启桌面应用。
# API_PROXY_PORT=37421

# 主进程日志目录（自动生成）：开发为项目根 log/；Windows 安装版为与已安装 exe 同目录下的 log/（若在 Program Files 下可能需管理员权限，可改用 userData，见 getLogPaths）；macOS 为 userData/log（见应用内 getLogPaths）。

# 桌面打包后：模板为应用目录下 resources/.env.example（与 exe 同级的 resources 文件夹内，mac 为 .app/Contents/Resources/.env.example）；复制为同目录 .env 后编辑。仓库不随包分发 .env（避免泄露密钥）。
